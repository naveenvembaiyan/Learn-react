import React from 'react';
import { LoginTitle } from '@/styles/Login.styled';

const Login = () => {
  return (
    <div>
      <LoginTitle className='text-white text-3xl text-center'>Login</LoginTitle>
    </div>
  );
};

export default Login;
